#include <stdio.h>

// 1) Defina a funcao soma
int soma(int x, int y) {
    return x+y;
}

// 2) Compile o codigo e suba as modificações para o repositorio origin

// 3) Nao permitir que arquivos com extensao .out sejam commitados 

int main() {
    printf("3 + 2 = %d\n", soma(3, 2));
    return 0;
}
